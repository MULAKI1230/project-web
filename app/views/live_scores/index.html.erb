<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home_lapangbola.com</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="max-w-xs mx-auto bg-white shadow-lg rounded-lg overflow-hidden">
      <div class="bg-gray-200 px-4 py-2">
        <h2 class="text-lg font-bold text-gray-700">INFO SKOR</h2>
      </div>
      <div class="divide-y divide-gray-300">
        <% @live_scores.each do |score| %>
          <div class="p-4 grid grid-cols-3 text-center">
            <div class="text-sm font-semibold"><%= score.home_team %></div>
            <div class="text-xl font-bold"><%= score.home_score %> - <%= score.away_score %></div>
            <div class="text-sm font-semibold"><%= score.away_team %></div>
          </div>
        <% end %>
      </div>
    </div>