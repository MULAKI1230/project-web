<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home_lapangbola.com</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100">
    <div class="container mx-auto px-4">
      <h1 class="text-2xl font-bold my-4">BERITA UTAMA HARI INI</h1>

      <% if @articles.any? %>
        <div class="bg-white shadow-lg rounded-lg p-4 mb-6">
          <h2 class="text-xl font-semibold"><%= @articles.first.title %></h2>
          <img src="<%= @articles.first.image %>" alt="<%= @articles.first.title %>" class="w-full h-48 object-cover rounded-lg my-2">
          <p class="text-gray-600"><%= truncate(@articles.first.content, length: 150) %></p>
          <a href="<%= article_path(@articles.first) %>" class="text-blue-500 hover:underline mt-2 block">Baca Selengkapnya</a>
        </div>
      <% end %>

      <div class="bg-gray-100 p-4 rounded-lg shadow-md mb-6">
        <h2 class="text-xl font-semibold text-gray-700">INFO SKOR</h2>
        <% @live_scores.each do |score| %>
          <div class="border rounded-lg p-3 mb-2 flex items-center justify-between">
            <span class="font-medium"><%= score.team_home %></span>
            <span class="text-lg font-bold"><%= score.score_home %> - <%= score.score_away %></span>
            <span class="font-medium"><%= score.team_away %></span>
          </div>
        <% end %>
      </div>

      <div class="max-w-3xl mx-auto bg-white shadow-md rounded-lg p-4 mt-8">
        <div class="text-xl font-bold mb-4 flex justify-between items-center">
          BERITA HARIAN
          <span class="cursor-pointer">▼</span>
        </div>

        <% @articles.each do |article| %>
          <div class="border-b pb-4 mb-4 flex gap-4">
            <img src="<%= article.image %>" alt="Gambar" class="w-24 h-24 rounded-lg object-cover">
            <div>
              <p class="text-sm text-gray-500"><%= article.published_at.strftime("%d %b %Y") %></p>
              <h3 class="font-bold"><%= article.title %></h3>
              <p class="text-gray-600 text-sm"><%= truncate(article.content, length: 100) %></p>
              <a href="<%= article_path(article) %>" class="text-blue-500 font-semibold">Baca Selengkapnya</a>
            </div>
          </div>
        <% end %>

        <div class="text-center">
          <button class="text-blue-500 font-semibold">Muat lebih banyak</button>
        </div>
      </div>
    </div>
  </body>
</html>